# src/queries/StreamsQuery.graphql
query StreamsQuery($sender: String!) {
  streams(where: { sender: $sender }) {
    id
    currentFlowRate
    createdAtTimestamp
    receiver {
      id
    }
  }
}
